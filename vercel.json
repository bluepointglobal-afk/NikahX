{
  "framework": null,
  "buildCommand": "cd frontend-test && npm install && npm run build",
  "outputDirectory": "frontend-test/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
