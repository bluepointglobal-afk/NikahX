{
  "framework": null,
  "buildCommand": "cd frontend-test && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "frontend-test/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
